# Copia este archivo como ".env" y ajusta los valores a tu entorno local.
# El archivo ".env" real NUNCA debe subirse al repositorio (ver .gitignore).
DB_URL=jdbc:mysql://localhost:3306/appdrones?useSSL=false&serverTimezone=UTC
DB_USER=root
DB_PASSWORD=1234
